import FullpageContainer from "@/components/Home/FullpageContainer";

const Home = () => {
  return (
    <main className="relative">
      <FullpageContainer />
    </main>
  );
};

export default Home;
